version: '3.4'
networks:
  kurkuma-network:
    external: true

services:
  healthy-studio:
    extends:
      file: docker-compose.base.yml
      service: healthy-studio
    build:
      dockerfile: docker/configs/healthy-studio/Dockerfile.test
    environment:
      NODE_ENV: test
      JWT_SECRET: ${JWT_SECRET}
      SPECS_DATA_AMOUNT: ${SPECS_DATA_AMOUNT}
      SPECS_HOOK_TIMEOUT_MS: ${SPECS_HOOK_TIMEOUT_MS}
    image: ${DOCKER_PREFIX}:healthy-studio
    container_name: healthy-studio-test
    command: yarn test:quick healthy-studio
